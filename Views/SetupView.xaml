<UserControl x:Class="SoulExe.Views.SetupView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource SheetSectionElevated}" Padding="22">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <!-- Only the active step is materialized in the shared scroll region. -->
                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsInitialSetupEngineStep}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <DockPanel Margin="0,0,0,6">
                            <Border DockPanel.Dock="Left" Background="{StaticResource AccentSoftBrush}" CornerRadius="8" Padding="10,4" VerticalAlignment="Center"><TextBlock Text="Шаг 1 / 2" FontSize="11" FontWeight="SemiBold" /></Border>
                            <TextBlock Text="Выберите движок" FontSize="22" FontWeight="SemiBold" Margin="14,0,0,0" VerticalAlignment="Center" />
                        </DockPanel>
                        <TextBlock Text="Движок llama.cpp запускает модель локально. Файлы ставятся только в SoulExeData рядом с программой — без системных установок." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="13" Margin="0,4,0,10" />
                        <TextBlock Text="1. Выберите вариант, затем установите его ниже." Style="{StaticResource SectionLabel}" Margin="0,2,0,4" />
                        <ListBox x:Name="SetupInitialFocus" ItemsSource="{Binding LlamaBackends}" SelectedItem="{Binding SelectedLlamaBackend}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="3" /></ItemsPanelTemplate></ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}"><Setter Property="Margin" Value="5" /><Setter Property="Padding" Value="0" /><Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="Background" Value="Transparent" /></Style></ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="Card" Style="{StaticResource GalleryCard}" Background="{StaticResource CardGradientBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1.5" CornerRadius="12" Padding="14" MinHeight="164">
                                        <Grid>
                                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                            <DockPanel>
                                                <Border DockPanel.Dock="Right" Background="{StaticResource SuccessBrush}" CornerRadius="6" Padding="8,3" VerticalAlignment="Top"><Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsInstalled}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Border.Style><TextBlock Text="Уже установлено" FontSize="10" FontWeight="SemiBold" Foreground="White" /></Border>
                                                <TextBlock Text="{Binding DisplayName}" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            </DockPanel>
                                            <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11.5" TextWrapping="Wrap" Margin="0,8,0,8" LineHeight="17" />
                                            <TextBlock Grid.Row="2" Text="{Binding ExampleHardware}" Foreground="{StaticResource AccentHoverBrush}" FontSize="10.5" TextWrapping="Wrap" Margin="0,0,0,8" />
                                            <TextBlock Grid.Row="3" Text="{Binding Id}" Foreground="{StaticResource TextDimBrush}" FontSize="10" FontFamily="Consolas" />
                                        </Grid>
                                    </Border>
                                    <DataTemplate.Triggers><DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True"><Setter TargetName="Card" Property="BorderBrush" Value="{StaticResource AccentBrush}" /><Setter TargetName="Card" Property="BorderThickness" Value="2.5" /><Setter TargetName="Card" Property="Background" Value="{StaticResource AccentSoftBrush}" /></DataTrigger></DataTemplate.Triggers>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Border Style="{StaticResource SystemStatusCard}" Padding="16" Margin="0,14,0,0">
                            <Grid x:Name="SetupEngineInstallLayout">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="2. Установите выбранный движок" Style="{StaticResource SectionLabel}" />
                                    <TextBlock Text="{Binding SelectedLlamaBackend.DisplayName}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0" />
                                    <TextBlock Text="{Binding SelectedBackendInstallState}" Foreground="{StaticResource AccentHoverBrush}" FontSize="12" Margin="0,3,0,0" />
                                    <TextBlock Text="{Binding SelectedBackendDescription}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,6,0,0" />
                                    <TextBlock Text="После установки перейдите к выбору модели через кнопку «Далее» внизу." Foreground="{StaticResource SuccessTextBrush}" TextWrapping="Wrap" FontSize="12" Margin="0,8,0,0" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0" MinWidth="200"><Button Content="Установить движок" Command="{Binding SetupInstallEngineCommand}" Style="{StaticResource AccentButton}" HorizontalAlignment="Stretch" Padding="16,12" /><TextBlock Text="Прогресс установки" Style="{StaticResource SectionLabel}" Margin="0,10,0,3" /><TextBlock Text="{Binding SetupProgressText}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,4" /><ProgressBar Minimum="0" Maximum="100" Value="{Binding SetupProgressPercent, Mode=OneWay}" Height="7" /></StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                    <Grid x:Name="SetupModelLayout">
                        <Grid.Style><Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsInitialSetupModelStep}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Grid.Style>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="1.15*" /><ColumnDefinition Width="16" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <Grid x:Name="SetupModelListPanel">
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                            <DockPanel Margin="0,0,0,6"><Border DockPanel.Dock="Left" Background="{StaticResource AccentSoftBrush}" CornerRadius="8" Padding="10,4" VerticalAlignment="Center"><TextBlock Text="Шаг 2 / 2" FontSize="11" FontWeight="SemiBold" /></Border><TextBlock Text="Выберите модель" FontSize="22" FontWeight="SemiBold" Margin="14,0,0,0" VerticalAlignment="Center" /></DockPanel>
                            <DockPanel Grid.Row="1" Margin="0,0,0,8"><TextBlock Text="{Binding RecommendedModels.Count, StringFormat=1. Выберите из рекомендаций: {0}}" Foreground="{StaticResource AccentHoverBrush}" FontSize="11" /><TextBlock DockPanel.Dock="Right" Text="Полный список моделей" Foreground="{StaticResource TextMutedBrush}" FontSize="10" /></DockPanel>
                            <ListBox Grid.Row="2" ItemsSource="{Binding RecommendedModels}" SelectedItem="{Binding SelectedRecommendedModel}" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
                                <ListBox.ItemTemplate><DataTemplate><Border Background="{StaticResource CardGradientBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,3"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" TextTrimming="CharacterEllipsis" /><Border Grid.Column="1" Background="{StaticResource ElevatedBrush}" CornerRadius="5" Padding="7,2" Margin="10,0,0,0"><TextBlock Text="{Binding OptimalQuant}" Foreground="{StaticResource AccentHoverBrush}" Style="{StaticResource SectionLabel}" /></Border><WrapPanel Grid.Row="1" Margin="0,5,0,0"><TextBlock Text="{Binding Author, StringFormat=Автор: {0}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" /><TextBlock Text="{Binding Downloads, StringFormat=  •  Загрузки: {0:N0}}" Foreground="{StaticResource TextMutedBrush}" FontSize="10" /><TextBlock Text="{Binding HardwareSummary}" Foreground="{StaticResource SuccessTextBrush}" FontSize="10" Margin="12,0,0,0" TextTrimming="CharacterEllipsis" MaxWidth="390" /></WrapPanel><Border Grid.Row="1" Grid.Column="1" Background="{StaticResource SuccessBrush}" CornerRadius="5" Padding="7,2" Margin="10,5,0,0"><Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsInstalled}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Border.Style><TextBlock Text="Скачано" Foreground="{StaticResource SuccessTextBrush}" FontSize="9" FontWeight="Bold" /></Border></Grid></Border></DataTemplate></ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                        <Border Grid.Column="2" Style="{StaticResource SheetSectionElevated}" Padding="18"><ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel><TextBlock Text="2. Скачайте выбранную модель" Style="{StaticResource SectionLabel}" /><TextBlock Text="{Binding SelectedRecommendedModel.Name}" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,8,0,0" /><TextBlock Text="{Binding SelectedRecommendedModel.Author, StringFormat=Автор: {0}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,5,0,0" /><TextBlock Text="{Binding SelectedRecommendedModel.Description}" TextWrapping="Wrap" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12.5" Margin="0,12,0,0" LineHeight="19" /><Border Background="{StaticResource CardGradientBrush}" CornerRadius="10" Padding="12" Margin="0,14,0,0"><StackPanel><TextBlock Text="{Binding SelectedRecommendedModel.QuantSummary}" Foreground="{StaticResource AccentHoverBrush}" TextWrapping="Wrap" FontSize="12" /><TextBlock Text="{Binding SelectedRecommendedModel.HardwareSummary}" Foreground="{StaticResource SuccessTextBrush}" TextWrapping="Wrap" FontSize="12" Margin="0,6,0,0" /><TextBlock Text="{Binding SelectedRecommendedModel.BeginnerHint}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" FontSize="12" Margin="0,6,0,0" /><TextBlock Text="{Binding RecommendedModelSizeInfo}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,6,0,0" /></StackPanel></Border><TextBlock Text="Прогресс скачивания" Style="{StaticResource SectionLabel}" Margin="0,16,0,3" /><TextBlock Text="{Binding SetupProgressText}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,0,4" /><ProgressBar Minimum="0" Maximum="100" Value="{Binding SetupModelDownloadPercent, Mode=OneWay}" Height="7" /><Button Content="Скачать модель" Command="{Binding SetupDownloadRecommendedCommand}" Style="{StaticResource AccentButton}" Margin="0,14,0,0" HorizontalAlignment="Stretch" Padding="14,12" /><Button Content="{Binding ModelDownloadPauseResumeText}" Command="{Binding ToggleModelDownloadCommand}" Style="{StaticResource CompactButton}" Background="{StaticResource WarningBrush}" Margin="0,8,0,0" ToolTip="Во время загрузки — пауза; после паузы — продолжить" /><Button Content="3. Запустить и открыть чат" Command="{Binding SetupStartChatCommand}" Style="{StaticResource SuccessButton}" Margin="0,10,0,0" HorizontalAlignment="Stretch" Padding="14,12" /><TextBlock Text="{Binding ModelDownloadStatus}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,10,0,0" /></StackPanel></ScrollViewer></Border>
                    </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource SystemStatusCard}" Padding="16,12" Margin="0,12,0,0">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding SelectedBackendInstallState, StringFormat=Движок: {0}}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsInitialSetupEngineStep}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
                    <Button Content="Пропустить начальную настройку" Command="{Binding SkipInitialSetupCommand}" Margin="16,0,0,0"><Button.Style><Style TargetType="Button" BasedOn="{StaticResource CompactButton}"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsInitialSetupVisible}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Button.Style></Button>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="← Назад к движку" Command="{Binding PreviousInitialSetupStepCommand}" Padding="16,11" Margin="0,0,10,0"><Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsInitialSetupModelStep}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Button.Style></Button><Button Content="Далее → выбор модели" Command="{Binding NextInitialSetupStepCommand}" Padding="18,11"><Button.Style><Style TargetType="Button" BasedOn="{StaticResource SuccessButton}"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsInitialSetupEngineStep}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Button.Style></Button></StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
