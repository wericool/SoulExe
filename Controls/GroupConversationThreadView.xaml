<UserControl x:Class="SoulExe.Controls.GroupConversationThreadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vm="clr-namespace:SoulExe.ViewModels"
              xmlns:ui="clr-namespace:SoulExe"
              xmlns:views="clr-namespace:SoulExe.Views"
              xmlns:controls="clr-namespace:SoulExe.Controls">
<Border Grid.Column="2" Panel.ZIndex="20" Background="{Binding ChatAppearance.ChatBackgroundColor}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="16" Padding="0" ClipToBounds="True">
    <Border.Style>
        <Style TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSceneChatActive}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Border.Style>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Background="Transparent" Padding="8,76,12,4">
            <Grid>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" MinWidth="0" Visibility="Collapsed">
                        <Grid Width="63" Height="40">
                            <Ellipse Width="40" Height="40" HorizontalAlignment="Left" Fill="{StaticResource AccentSoftBrush}" />
                            <TextBlock Text="{Binding SelectedSceneCharacterA.Initials}" Width="40" HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Center" FontSize="11" FontWeight="Bold" Foreground="White" />
                            <Ellipse Width="40" Height="40" HorizontalAlignment="Left">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding SelectedSceneCharacterA.AvatarPath}" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            <Ellipse Width="28" Height="28" HorizontalAlignment="Right" VerticalAlignment="Bottom" Fill="{StaticResource AccentSoftBrush}" Stroke="{StaticResource PanelBrush}" StrokeThickness="2" />
                            <TextBlock Text="{Binding SelectedSceneCharacterB.Initials}" Width="28" Height="28" HorizontalAlignment="Right" VerticalAlignment="Bottom" TextAlignment="Center" Padding="0,8,0,0" FontSize="8" FontWeight="Bold" Foreground="White" IsHitTestVisible="False" />
                            <Ellipse Width="28" Height="28" HorizontalAlignment="Right" VerticalAlignment="Bottom" Stroke="{StaticResource PanelBrush}" StrokeThickness="2" IsHitTestVisible="False">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding SelectedSceneCharacterB.AvatarPath}" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                        </Grid>
                        <StackPanel Margin="11,0,0,0" VerticalAlignment="Center" MinWidth="0">
                            <TextBlock Text="{Binding SceneParticipantNames}" FontWeight="SemiBold" FontSize="15" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding SceneLastMessageLabel}" Foreground="{StaticResource TextMutedBrush}" FontSize="10.5" Margin="0,3,0,0" TextTrimming="CharacterEllipsis" MaxWidth="400" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                        <TextBlock Text="{Binding SelectedGroupConversation.Name}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="110" Margin="0,0,8,0" />
                        <Button Content="в°" Command="{x:Static views:ChatWorkspaceView.ShowConversationListCommand}" Style="{StaticResource CircleIconButton}" AutomationProperties.Name="РћС‚РєСЂС‹С‚СЊ СЃРїРёСЃРѕРє РґРёР°Р»РѕРіРѕРІ" ToolTip="Р”РёР°Р»РѕРіРё" Margin="0,0,6,0" />
                        <Button Content="в“" Command="{x:Static views:ChatWorkspaceView.ShowConversationDetailsCommand}" Style="{StaticResource CircleIconButton}" AutomationProperties.Name="РћС‚РєСЂС‹С‚СЊ РїР°СЂР°РјРµС‚СЂС‹ СЂР°Р·РіРѕРІРѕСЂР°" ToolTip="РџР°СЂР°РјРµС‚СЂС‹ СЂР°Р·РіРѕРІРѕСЂР°" Margin="0,0,6,0" />
                        <Button Content="вЊ•" Command="{Binding ToggleSceneMessageSearchCommand}" Style="{StaticResource CircleIconButton}" AutomationProperties.Name="РџРѕРёСЃРє РїРѕ СЂРµРїР»РёРєР°Рј" ToolTip="РџРѕРёСЃРє РїРѕ СЂРµРїР»РёРєР°Рј СЌС‚РѕРіРѕ СЂР°Р·РіРѕРІРѕСЂР°" Margin="0,0,6,0" />
                        <Button Content="..." Style="{StaticResource CircleIconButton}" AutomationProperties.Name="Р”РµР№СЃС‚РІРёСЏ СЃ СЂР°Р·РіРѕРІРѕСЂРѕРј" ToolTip="Р”РµР№СЃС‚РІРёСЏ СЃ СЂР°Р·РіРѕРІРѕСЂРѕРј" Click="MessageMenuButton_OnClick" Tag="{Binding}">
                            <Button.ContextMenu>
                                <ContextMenu Style="{StaticResource ChatActionContextMenu}" Placement="Bottom" StaysOpen="False">
                                    <MenuItem Header="{Binding PlacementTarget.Tag.SelectedConversationItem.PinMenuText, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Style="{StaticResource ChatActionMenuItem}" Command="{Binding PlacementTarget.Tag.ToggleConversationPinnedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.Tag.SelectedConversationItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="вњЋ  РџРµСЂРµРёРјРµРЅРѕРІР°С‚СЊ" Style="{StaticResource ChatActionMenuItem}" Command="{Binding PlacementTarget.Tag.BeginRenameConversationCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.Tag.SelectedConversationItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <Separator />
                                    <MenuItem Header="рџ—‘  РЈРґР°Р»РёС‚СЊ СЂР°Р·РіРѕРІРѕСЂ" Style="{StaticResource ChatActionMenuItem}" Foreground="{StaticResource DangerHoverBrush}" Command="{Binding PlacementTarget.Tag.DeleteConversationCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.Tag.SelectedConversationItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                    </StackPanel>
                </Grid>
                <Border HorizontalAlignment="Center" VerticalAlignment="Center" Background="{StaticResource ElevatedBrush}" BorderBrush="{StaticResource AccentSoftBrush}" BorderThickness="1" CornerRadius="9" Padding="11,6" Panel.ZIndex="2">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSceneCountdownVisible}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="в—·" Foreground="{StaticResource AccentHoverBrush}" FontSize="13" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding SceneCountdownText}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="11.5" FontWeight="SemiBold" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Border Grid.Row="1" Panel.ZIndex="12" MaxWidth="340" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20,14,20,0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="12" Padding="12" Effect="{StaticResource AccentGlowEffect}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSceneMessageSearchOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="Г—" Command="{Binding CloseSceneMessageSearchCommand}" Style="{StaticResource CircleIconButton}" AutomationProperties.Name="Р—Р°РєСЂС‹С‚СЊ РїРѕРёСЃРє" ToolTip="Р—Р°РєСЂС‹С‚СЊ РїРѕРёСЃРє" />
                    <TextBlock Text="РџРѕРёСЃРє РІ РіСЂСѓРїРїРѕРІРѕРј СЂР°Р·РіРѕРІРѕСЂРµ" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" />
                </DockPanel>
                <Border Grid.Row="1" Background="{StaticResource InputBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="9" Margin="0,10,0,8" Padding="9,0">
                    <DockPanel>
                        <TextBlock Text="вЊ•" Foreground="{StaticResource TextMutedBrush}" FontSize="14" Margin="0,0,7,0" VerticalAlignment="Center" />
                        <TextBox Text="{Binding SceneMessageSearchQuery, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" MinHeight="32" Padding="0" VerticalContentAlignment="Center" ToolTip="Р’РІРµРґРёС‚Рµ С‚РµРєСЃС‚ РґР»СЏ РїРѕРёСЃРєР° РїРѕ СЂРµРїР»РёРєР°Рј" />
                    </DockPanel>
                </Border>
                <ScrollViewer Grid.Row="2" MaxHeight="260" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding SceneMessageSearchResults}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:ChatMessageSearchResult}">
                                <Button Command="{Binding DataContext.SelectSceneMessageSearchResultCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Background="Transparent" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="5,8" HorizontalContentAlignment="Stretch" ToolTip="РџРµСЂРµР№С‚Рё Рє РЅР°Р№РґРµРЅРЅРѕР№ СЂРµРїР»РёРєРµ">
                                    <StackPanel>
                                        <DockPanel>
                                            <TextBlock Text="{Binding Timestamp}" DockPanel.Dock="Right" FontSize="9.5" Foreground="{StaticResource TextMutedBrush}" />
                                            <TextBlock Text="{Binding DisplayAuthor}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource AccentHoverBrush}" />
                                        </DockPanel>
                                        <TextBlock Text="{Binding Preview}" Margin="0,4,0,0" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="42" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
        <Grid Grid.Row="1">
            <ListBox x:Name="SceneMessagesList" ItemsSource="{Binding SceneMessages}" Style="{StaticResource ConversationMessageList}" ScrollViewer.ScrollChanged="SceneMessagesScroll_OnScrollChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Focusable" Value="False" />
                                <Setter Property="IsHitTestVisible" Value="True" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:SceneMessageViewModel}">
                                <Border x:Name="SceneBubble" Margin="0,7" Padding="12,10" CornerRadius="{Binding DataContext.ChatAppearance.BubbleCornerRadius, RelativeSource={RelativeSource AncestorType=Window}}" Background="{Binding DataContext.ChatAppearance.AssistantBubbleColor, RelativeSource={RelativeSource AncestorType=Window}}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" MaxWidth="{Binding DataContext.ChatAppearance.BubbleMaxWidth, RelativeSource={RelativeSource AncestorType=Window}}" HorizontalAlignment="Left">
                                    <StackPanel>
                                        <DockPanel LastChildFill="True" Margin="0,0,0,4">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Border x:Name="SceneAvatar" Width="24" Height="24" CornerRadius="12" Background="{StaticResource AccentSoftBrush}" Margin="0,0,7,0">
                                                    <Grid>
                                                        <TextBlock Text="{Binding SpeakerInitials}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="8.5" FontWeight="Bold" Foreground="White" />
                                                        <Ellipse IsHitTestVisible="False">
                                                            <Ellipse.Fill>
                                                                <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill" />
                                                            </Ellipse.Fill>
                                                        </Ellipse>
                                                    </Grid>
                                                </Border>
                                                <TextBlock x:Name="SceneSpeaker" Text="{Binding SpeakerName}" FontWeight="SemiBold" FontSize="11.5" Foreground="{StaticResource AccentHoverBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                        </DockPanel>
                                        <DockPanel LastChildFill="True" Margin="0,1,0,0">
                                            <TextBlock x:Name="SceneTime" DockPanel.Dock="Right" Text="{Binding Time}" Foreground="{StaticResource TextMutedBrush}" FontSize="9" VerticalAlignment="Bottom" Margin="8,0,0,1" />
                                            <ui:SelectableFormattedChatBox x:Name="SceneText" ContentText="{Binding Content}" Appearance="{Binding DataContext.ChatAppearance, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,2,0" />
                                        </DockPanel>
                                    </StackPanel>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsSearchHighlighted}" Value="True">
                                        <Setter TargetName="SceneBubble" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}" />
                                        <Setter TargetName="SceneBubble" Property="BorderThickness" Value="2" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsFirstCharacter}" Value="True">
                                        <Setter TargetName="SceneBubble" Property="HorizontalAlignment" Value="Right" />
                                        <Setter TargetName="SceneBubble" Property="Background" Value="{Binding DataContext.ChatAppearance.UserBubbleColor, RelativeSource={RelativeSource AncestorType=Window}}" />
                                        <Setter TargetName="SceneBubble" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                        <Setter TargetName="SceneText" Property="Foreground" Value="{StaticResource TextOnAccentBrush}" />
                                        <Setter TargetName="SceneSpeaker" Property="Foreground" Value="{StaticResource TextOnAccentBrush}" />
                                        <Setter TargetName="SceneAvatar" Property="Background" Value="{StaticResource AccentBrush}" />
                                        <Setter TargetName="SceneTime" Property="Foreground" Value="{StaticResource ChatOutboundMetaBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsUserParticipant}" Value="True">
                                        <Setter TargetName="SceneBubble" Property="HorizontalAlignment" Value="Center" />
                                        <Setter TargetName="SceneBubble" Property="Background" Value="{StaticResource ElevatedBrush}" />
                                        <Setter TargetName="SceneBubble" Property="BorderBrush" Value="{StaticResource AccentSoftBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsDirector}" Value="True">
                                        <Setter TargetName="SceneBubble" Property="HorizontalAlignment" Value="Center" />
                                        <Setter TargetName="SceneBubble" Property="Background" Value="{StaticResource ElevatedBrush}" />
                                        <Setter TargetName="SceneAvatar" Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="24,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SceneMessages.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
                Р РµРїР»РёРє РїРѕРєР° РЅРµС‚. Р”РѕР±Р°РІСЊС‚Рµ РїРµСЂРІСѓСЋ СЂРµРїР»РёРєСѓ С‡РµСЂРµР· РїРѕР»Рµ РІРІРѕРґР° РЅРёР¶Рµ.
            </TextBlock>
            <Border Background="{Binding ChatAppearance.AssistantBubbleColor}" VerticalAlignment="Bottom" Margin="24,0,24,12">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource ConversationTypingBubble}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSceneTyping}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="24" Height="24" CornerRadius="12" Background="{StaticResource AccentSoftBrush}" Margin="0,0,8,0">
                                <TextBlock Text="вЂ¦" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" />
                            </Border>
                            <TextBlock Text="РџРµСЂСЃРѕРЅР°Р¶ РїРµС‡Р°С‚Р°РµС‚вЂ¦" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic" />
                        </StackPanel>
            </Border>
            <Button Content="Р—Р°РіСЂСѓР·РёС‚СЊ РїСЂРµРґС‹РґСѓС‰РёРµ СЃРѕРѕР±С‰РµРЅРёСЏ" Command="{Binding LoadOlderSceneMessagesCommand}" Click="LoadOlderMessagesButton_OnClick" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,12,0,0">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource GhostButton}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers><DataTrigger Binding="{Binding HasOlderSceneMessages}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
        <Button x:Name="NewMessagesButton" Grid.Row="1" Panel.ZIndex="11" Content="РќРѕРІС‹Рµ СЃРѕРѕР±С‰РµРЅРёСЏ" Click="NewMessagesButton_OnClick" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,24,20" Padding="14,8" Style="{StaticResource AccentButton}" Visibility="Collapsed" AutomationProperties.Name="РќРѕРІС‹Рµ СЃРѕРѕР±С‰РµРЅРёСЏ" ToolTip="РџРµСЂРµР№С‚Рё Рє РЅРѕРІС‹Рј СЃРѕРѕР±С‰РµРЅРёСЏРј" />
        <controls:ConversationComposerView Grid.Row="2" Tag="Group" />
    </Grid>
</Border>
</UserControl>
